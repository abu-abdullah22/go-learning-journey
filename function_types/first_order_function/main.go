package main

import "fmt"

func main() {
	fmt.Println("This is a first-order function example in Go.")

	// what is a first-order function?
	// A first-order function is a function that does not take other functions as arguments and does not return a function as its result. In Go, functions are first-class citizens, meaning they can be assigned to variables, passed as arguments, and returned from other functions. However, a first-order function specifically refers to functions that operate on basic data types rather than other functions.
}
